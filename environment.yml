name: aligner
channels:
  - conda-forge
dependencies:
  - python=3.10
  - montreal-forced-aligner
  - librosa
  - pysoundfile
  - pillow
  - pip
  - pip:
      - flask
      - flask-cors
      - praat-parselmouth